Prompt Log (Fill in during work)

1. 请基于现有原型增加“流程进度提示”，在每个页面标题下加一个高层步骤指示（例如 Overview → Action → Confirmation），并保持简洁不占空间。
2. 优化状态反馈：冲突修复后列表项要有明显的已解决样式，注册成功后卡片应显示“Confirmed”徽章。
3. 增加搜索的加载状态和空结果状态，输入少于 3 个字符要提示，搜索中显示“Searching...”，无结果时给出建议。
4. 强化可访问性与易用性：按钮/卡片要有 hover 与 focus 样式，提升视觉反馈。
